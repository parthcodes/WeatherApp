<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:layout_gravity="center"
    tools:context="com.example.parth.weatherapp.SettingsActivity"
    >


    <Spinner
        android:id="@+id/spinner_language"
        android:layout_marginTop="20dp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:entries="@array/language_arrays"
        android:prompt="@string/language_default">
        </Spinner>


    <Spinner
        android:id="@+id/spinner_temprature"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/spinner_language"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:entries="@array/temperature_arrays"
        android:prompt="@string/temperature_default">
    </Spinner>

    <Spinner
        android:id="@+id/spinner_days"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/spinner_temprature"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:prompt="@string/days_default"
        android:entries="@array/days_arrays">

    </Spinner>
<Button
    android:id="@+id/button_submit"
    android:layout_width="100dp"
    android:layout_height="wrap_content"
    android:text="Save"
    android:layout_below="@+id/spinner_days"

    android:layout_marginTop="40dp"/>



</RelativeLayout>
